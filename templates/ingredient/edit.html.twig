{% extends 'baseBack.html.twig' %}

{% block title %}Edit Ingredient{% endblock %}

{% block content %}
<style>
.text-right {
    text-align: right;
    margin-right: 800px; /* Adjust margin as needed */
    margin-top: -28px; /* Adjust margin-top to move the button up */
}
</style>
    <h1>Edit Ingredient</h1>

    {{ include('ingredient/_form.html.twig', {'button_label': 'Update'}) }}
<div class="text-right"> <!-- Aligns content to the right -->
        <a href="{{ path('app_ingredient_index') }}"class="btn btn-inverse-warning btn-fw">Back to list</a>
    </div>

{% endblock %}
